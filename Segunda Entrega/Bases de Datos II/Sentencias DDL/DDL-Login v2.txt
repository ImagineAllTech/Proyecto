create table Persona(
CI int(8),
Nombre varchar(20),
Apellido varchar(20),
primary key(CI)
);

create table PersonaTelefono(
CI int(8),
Telefono int(9),
foreign key(CI) references Persona(CI)
);

create table Administrador(
CI int(8),
foreign key(CI) references Persona(CI)
);

create table Juez(
CI int(8),
foreign key(CI) references Persona(CI)
);

create table Usuario(
ID int(8) auto_increment,
Nombre varchar(20),
Contraseña varchar(50),
Rol set('Juez', 'Administrador'),
primary key(ID)
);

create table Tiene(
CI_adm int(8),
ID int(8),
Foreign key(CI_adm) references Administrador(CI)
);

create table Posee(
CI_juez int(8),
ID int(8),
Foreign key(CI_juez) references Juez(CI)
);